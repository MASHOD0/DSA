#include<stdio.h>
#include<math.h>
int main(void){
    for(int i = 1 ; i <= 100; i++){
        int amstrong, sum, arr[3];
        int num = i;
        while(n > 0){
            int j = 0;
            r = num % 10;
            arr[j] = r;
            num = num / 10;
        }
        for (int k = 0 ; k < 2; k++){
            sum += pow(arr[k], 3)
        }
        if(sum == i)
        amstrong++;
    }
    return 0;
}